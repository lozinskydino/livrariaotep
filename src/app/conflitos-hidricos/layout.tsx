import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Disponibilidade e conflitos por água",
};

export default function ConflitosHidricosLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
